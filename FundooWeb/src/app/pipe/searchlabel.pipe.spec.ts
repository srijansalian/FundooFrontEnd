import { SearchlabelPipe } from './searchlabel.pipe';

describe('SearchlabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchlabelPipe();
    expect(pipe).toBeTruthy();
  });
});
